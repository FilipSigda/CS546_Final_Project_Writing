<h2>Sign In</h2>

<form id="signin-form" name="signin-form" method="post" action="/users/signinuser">
    <label for="user_name">Enter Your Username<br></label>
    <input type="text" name="user_name" id="user_name"/><br>

    <label for="password">Enter Your Password</label><br>
    <input type="password" name="password" id="password"/><br>

    <button type="submit" name="submit_button" id="submit_button">Submit</button>
</form>

<!-- I am a missing error passed down from Server-side JS!-->
{{#if missing}}
    <div id="serverMissing" class="error">
        <p>The following fields are left empty:</p>
        <ul>
        {{#each missing}}
            <li>{{this}}</li>
        {{/each}}
        </ul>
    </div>
{{/if}}

<!--I am an error passed down from Server-Side JS! -->
{{#if error}}
    <div id="serverError" class="error">{{error}}</div>
{{/if}}

<div id="clientError" class="error" hidden>I'm an error passed down through Client-Side JS.</div>

<a href="/signupuser">Click here to signup!</a>